version: '2.3'
services:

  pub:
    image: publisher:dev
    build:
      context: '.'
      dockerfile: 'Dockerfile'
    tty: true
    command: ["python3", "/app/main.py", "--ip", "sub"]
    volumes:
      - ./:/app